# ── Core Framework ──────────────────────────────────────────────────────
fastapi==0.115.14
uvicorn==0.34.3
gunicorn==23.0.0
starlette==0.46.2
httptools==0.7.1
h11==0.16.0
python-multipart==0.0.20
python-dotenv==1.1.1
click==8.1.8

# ── Pydantic / Typing ──────────────────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.12.0
pydantic_core==2.41.5
annotated-types==0.7.0
typing-inspection==0.4.2
typing_extensions==4.14.1

# ── Database (MySQL) ───────────────────────────────────────────────────
SQLAlchemy==2.0.41
sqlmodel==0.0.24
mysql-connector-python==9.6.0
PyMySQL==1.1.1
aiomysql==0.3.2
greenlet==3.2.3

# ── HTTP / Networking ──────────────────────────────────────────────────
httpx==0.28.1
httpcore==1.0.9
httpx-sse==0.4.3
requests==2.32.4
urllib3==2.5.0
certifi==2025.7.14
charset-normalizer==3.4.2
idna==3.10
sniffio==1.3.1
anyio==4.9.0

# ── Async helpers ──────────────────────────────────────────────────────
aiohttp==3.13.2
aiohappyeyeballs==2.6.1
aiosignal==1.4.0
aiosqlite==0.21.0
async-timeout==5.0.1
frozenlist==1.8.0
multidict==6.7.0
propcache==0.4.1
yarl==1.22.0

# ── AI / LLM ──────────────────────────────────────────────────────────
openai==2.9.0
openai-agents==0.6.2
litellm==1.80.9
tiktoken==0.12.0
tokenizers==0.22.1
mcp==1.23.3
sse-starlette==3.0.3
jiter==0.12.0

# ── Hugging Face (image gen API) ───────────────────────────────────────
huggingface_hub==1.2.2
hf-xet==1.2.0
fsspec==2025.12.0
filelock==3.20.0

# ── Scraping / Parsing ────────────────────────────────────────────────
beautifulsoup4==4.13.4
soupsieve==2.7
lxml==5.4.0
markdownify==1.2.2
html5lib==1.1

# ── PDF Generation ────────────────────────────────────────────────────
xhtml2pdf==0.2.17
reportlab==4.4.2
pypdf==5.8.0
svglib==1.5.1
arabic-reshaper==3.0.0
python-bidi==0.6.6
freetype-py==2.5.1
cssselect2==0.8.0
tinycss2==1.4.0
webencodings==0.5.1

# ── Crypto / Signing (PDF, SSL) ───────────────────────────────────────
cryptography==3.4.8
cffi==1.17.1
pycparser==2.22
pyHanko==0.20.1
pyhanko-certvalidator==0.24.1
asn1crypto==1.5.1
oscrypto==1.3.0
PyJWT==2.10.1
qrcode==8.2
uritools==5.0.0

# ── Image (lightweight, for Pillow only) ───────────────────────────────
pillow==11.3.0

# ── Misc / Utilities ──────────────────────────────────────────────────
attrs==25.4.0
distro==1.9.0
exceptiongroup==1.3.0
griffe==1.15.0
jsonschema==4.25.1
jsonschema-specifications==2025.9.1
Jinja2==3.1.6
MarkupSafe==3.0.2
packaging==25.0
platformdirs==4.9.2
protobuf==6.33.5
Pygments==2.19.2
PyYAML==6.0.2
referencing==0.37.0
regex==2025.11.3
rpds-py==0.30.0
shellingham==1.5.4
six==1.17.0
tqdm==4.67.1
typer-slim==0.20.0
types-requests==2.32.4.20250913
tzdata==2025.2
tzlocal==5.3.1
websockets==15.0.1
zipp==3.23.0
annotated-doc==0.0.4
asgiref==3.9.1
fastuuid==0.14.0
numpy==2.2.6

# ── REMOVED for Azure deployment (optional heavy packages) ─────────────
# rembg==2.0.69          # ~170 MB model; bg_removal degrades gracefully
# onnxruntime==1.23.2    # ~200 MB; only needed by rembg
# scipy==1.15.3          # ~100 MB; only needed by rembg/scikit-image
# scikit-image==0.25.2   # only needed by rembg
# opencv-python-headless==4.13.0.92  # only needed by rembg
# PyMatting==1.1.15      # only needed by rembg
# pooch==1.9.0           # only needed by scikit-image
# tifffile==2025.5.10    # only needed by scikit-image
# lazy_loader==0.4       # only needed by scikit-image
# networkx==3.4.2        # only needed by scikit-image
# flatbuffers==25.12.19  # only needed by onnxruntime
# coloredlogs==15.0.1    # only needed by onnxruntime
# humanfriendly==10.0    # only needed by coloredlogs
# sympy==1.14.0          # only needed by onnxruntime
# mpmath==1.3.0          # only needed by sympy
# pyodbc==5.3.0          # requires system unixODBC headers; using MySQL
# psycopg2-binary==2.9.10  # PostgreSQL driver; not used
# asyncpg==0.31.0        # PostgreSQL async driver; not used
# grpcio==1.67.1         # not used directly
